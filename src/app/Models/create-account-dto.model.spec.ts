import { CreateAccountDto } from './create-account-dto.model';

describe('CreateAccountDto', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new CreateAccountDto()).toBeTruthy();
  });
});
