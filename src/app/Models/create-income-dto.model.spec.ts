// @ts-ignore
import { CreateIncomeeDto } from './create-income-dto.model';

describe('CreateIncomeeDto', () => {
  it('should create an instance', () => {
    expect(new CreateIncomeeDto()).toBeTruthy();
  });
});
