import { CreateExpenseDto } from './create-expense-dto.model';

describe('CreateExpenseDto', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new CreateExpenseDto()).toBeTruthy();
  });
});
